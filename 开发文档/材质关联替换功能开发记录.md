# 材质关联替换功能开发记录

## 功能概述

从外部 Blender 文件 **Link** 材质球到当前场景，建立映射关系后批量替换场景中所有对象的材质。

---

## 一、材质替换功能

### 交互流程

```
选择外部文件 → 勾选要Link的材质 → Link并生成映射 → 调整映射 → 执行替换
```

### 自动匹配规则

1. **完全同名匹配**：材质名（去掉数字后缀后）完全相同
2. **后缀匹配**：后缀相同（如 `Body`、`Weapon`、`Head`）但前缀不同
3. **未匹配**：显示"不替换"，用户可手动选择

---

## 二、关联动画功能

### 功能说明

从外部 Blender 文件 Link 名为 **Ani** 的集合到当前场景，并放置在世界中心。

### 交互流程

```
选择动画文件 → 检查Ani集合 → 关联到世界中心
```

### 关键 API

```python
# Link 集合
with bpy.data.libraries.load(filepath, link=True) as (data_from, data_to):
    data_to.collections = ["Ani"]

# 查找 Link 集合（注意：名称可能带文件路径前缀）
ani_collection = None
for coll in bpy.data.collections:
    if coll.library is not None:
        if coll.name == "Ani" or "Ani" in coll.name.split('@')[0]:
            ani_collection = coll
            break

# 创建集合实例
instance = bpy.data.objects.new("Ani", None)
instance.empty_display_type = 'PLAIN_AXES'
instance.instance_type = 'COLLECTION'
instance.instance_collection = ani_collection
instance.location = (0, 0, 0)
context.scene.collection.objects.link(instance)
```

**注意**：`empty_display_type` 和 `instance_type` 是两个不同属性：
- `empty_display_type`：空对象的视觉显示样式（坐标轴、箭头、圆等）
- `instance_type`：实例的数据类型（集合、物体等）

---

## 三、骨骼缩放控制功能

### 功能说明

给场景中的 Armature 骨骼添加 Copy Scale 约束，通过 Scale 空物体控制骨骼整体缩放。

### 交互流程

```
选择目标骨骼 → 创建约束 → 调整缩放值 → 启用/禁用约束
```

### UI布局

```
┌─────────────────────────────────────┐
│ [骨骼缩放]                            │
├─────────────────────────────────────┤
│ 骨骼: [下拉选择] [创建约束] ✓          │
│                                      │
│ 缩放: [滑块] (1.00)                   │
│ ☑ 偏移  叠加原有缩放                   │
└─────────────────────────────────────┘
```

### 关键 API

```python
# 创建 Copy Scale 约束
constraint = armature.constraints.new('COPY_SCALE')
constraint.name = "MMY_Copy_Scale"
constraint.target = scale_obj  # Scale 空物体
constraint.use_x = True
constraint.use_y = True
constraint.use_z = True
constraint.use_offset = True  # 偏移模式（叠加原有缩放）
constraint.owner_space = 'LOCAL'
constraint.target_space = 'LOCAL'
constraint.mute = False  # 启用/禁用约束
```

**约束参数说明**：
- `use_offset`：叠加在原有缩放上，而不是直接覆盖
- `mute`：True = 禁用约束，False = 启用约束

---

## 四、遇到的问题与解决方案

### 问题1：Link材质和本地材质同名导致替换失败

**现象**：
- 映射关系正确建立
- 点击替换后显示"已是目标材质，跳过"
- 模型对象的材质球没有变化

**原因**：
```python
# 错误写法：同名时 get() 返回本地材质，不是 Link 材质
target_mat = bpy.data.materials.get(target_name)
```

当源材质名 `502016_Body` 和目标材质名 `502016_Body` 完全相同时，`bpy.data.materials.get()` 会返回本地材质对象，而不是 Link 进来的材质。

**解决方案**：
```python
# 正确写法：通过 library 属性区分 Link 材质
target_mat = None
for mat in bpy.data.materials:
    if mat.name == target_name and mat.library is not None:
        target_mat = mat
        break
```

**关键点**：
- Link 材质的 `mat.library` 属性不为 `None`
- 本地材质的 `mat.library` 属性为 `None`
- 替换时需要比较材质**对象引用**，而不是名字

### 问题2：属性回调函数定义顺序错误

**现象**：
- 插件加载时报错：`NameError: name '_update_constraint_enabled' is not defined`
- 类定义中的 `update=_update_constraint_enabled` 找不到函数

**原因**：
```python
# 错误写法：函数定义在类后面
class MMY_MatReplacerProps(bpy.types.PropertyGroup):
    constraint_enabled: BoolProperty(
        update=_update_constraint_enabled  # 此时函数还未定义！
    )

def _update_constraint_enabled(self, context):  # 定义在类后面
    ...
```

Python 在类定义时会立即解析所有属性，此时 `_update_constraint_enabled` 函数尚未定义，导致报错。

**解决方案**：
```python
# 正确写法：函数定义在类前面
def _update_constraint_enabled(self, context):
    ...

class MMY_MatReplacerProps(bpy.types.PropertyGroup):
    constraint_enabled: BoolProperty(
        update=_update_constraint_enabled  # 函数已定义，可正常引用
    )
```

**关键点**：
- 所有作为 `update` 回调的函数必须在类定义**之前**声明
- 这是 Blender PropertyGroup 的常见陷阱

### 问题3：Git仓库与Blender插件目录文件不同步

**现象**：
- Git仓库代码已修复，但Blender仍报相同错误
- `git push` 后插件仍无法加载

**原因**：
Blender 插件目录 (`C:\BlenderAPP\...\scripts\addons\`) 和 Git 仓库 (`D:\GitWork\...`) 是两个独立位置，修改Git仓库不会自动更新Blender目录。

**解决方案**：
- 手动同步：修改后需要同时更新两处文件
- 或使用脚本自动同步
- 推送前确认 Blender 插件目录的文件也是最新版本

### 问题4：应用变换时set对象不能下标访问

**现象**：
- FBX导入勾选"清零变换"时报错：`'set' object is not subscriptable`
- 导入成功但变换未应用

**原因**：
```python
# 错误写法：set 对象不能用下标访问
after_objs = set(context.scene.objects)
new_objs = after_objs - before_objs  # new_objs 是 set 类型
bpy.context.view_layer.objects.active = new_objs[0]  # 报错！
```

Python 的 `set` 类型是无序集合，不支持下标索引 `[0]`。

**解决方案**：
```python
# 正确写法：先转为 list
obj_list = list(new_objects)
bpy.context.view_layer.objects.active = obj_list[0]
```

**关键点**：
- `set` 差集运算结果仍是 `set`，不能直接下标访问
- 需要下标访问时，先用 `list()` 转换
- 这是 Python 基础类型的常见陷阱

---

## 五、文件结构

```
mmy_toolkit/mat_replacer/
├── __init__.py      # 模块注册入口
├── properties.py    # 属性定义（材质映射、动画关联、骨骼缩放）
├── operators.py     # 操作符（材质替换、动画关联、骨骼约束）
└── ui/
    └── panels.py    # UI面板绘制
```

---

## 六、FBX导入增强

### 新增功能

在FBX导入工具中新增"清零变换"选项，导入后自动清除模型的位移、缩放、旋转。

### UI布局

```
┌─────────────────────────────────────┐
│ [导入工具]                            │
├─────────────────────────────────────┤
│ [导入FBX] ☑动画                       │
│ ☑引用材质 ☑清零变换                   │
└─────────────────────────────────────┘
```

### 关键代码

```python
# 属性定义（mesh_tools/__init__.py）
bpy.types.Scene.mmy_clear_transforms = bpy.props.BoolProperty(
    name="清零变换",
    description="导入后应用变换，将物体的变换结构应用为自身数据",
    default=True
)

# 应用变换方法（mesh_tools/operators.py）
def _clear_transforms(self, new_objects):
    # set 转 list（关键！）
    obj_list = list(new_objects)
    if not obj_list:
        return

    # 选中所有新导入的对象
    bpy.ops.object.select_all(action='DESELECT')
    for obj in obj_list:
        obj.select_set(True)

    # 设置第一个对象为活动对象
    bpy.context.view_layer.objects.active = obj_list[0]

    # 应用变换（位移、旋转、缩放）
    bpy.ops.object.transform_apply(location=True, rotation=True, scale=True)
```

**注意**：应用变换会将物体的位移、旋转、缩放"烘焙"到物体数据中，变换值归零但模型在世界空间的位置保持不变。

### 问题5：关联动画集合为空或找不到

**现象**：
- 点击"关联 Ani 集合"后，创建的集合实例为空
- 控制台报错：`Link 失败：未找到 Ani 集合`

**原因**：
```python
# 错误写法：直接用名称查找
ani_collection = bpy.data.collections.get("Ani")
```

当使用 `link=True` 加载集合时，Blender 可能会给集合名称添加文件路径前缀（如 `Ani@filepath`），导致 `get("Ani")` 找不到正确的集合。

**解决方案**：
```python
# 正确写法：遍历查找 Link 集合
ani_collection = None
for coll in bpy.data.collections:
    if coll.library is not None:  # 只查找 Link 的集合
        if coll.name == "Ani" or "Ani" in coll.name.split('@')[0]:
            ani_collection = coll
            break
```

**关键点**：
- Link 的集合 `library` 属性不为 `None`
- 集合名称可能被 Blender 修改，需要模糊匹配
- 添加调试输出查看集合实际名称和内容

---

## 七、变更记录

| 日期 | 内容 |
|------|------|
| 2026-05-19 | 新增材质关联替换功能，修复同名材质替换问题 |
| 2026-05-19 | 调整UI布局：操作按钮合并到一行，双列布局更紧凑美观 |
| 2026-05-19 | 新增关联动画功能：Link Ani集合到世界中心 |
| 2026-05-19 | 修复集合实例创建：empty_display_type使用PLAIN_AXES，instance_type使用COLLECTION |
| 2026-05-19 | 新增骨骼缩放控制：Copy Scale约束 + Scale空物体 + 滑块控制 |
| 2026-05-19 | 骨骼约束添加偏移选项和启用开关 |
| 2026-05-19 | FBX导入新增清零变换选项，默认勾选 |
| 2026-05-19 | 修复清零变换：改为应用变换（Apply Transform），修复set下标访问错误 |
| 2026-05-20 | 修复关联动画集合查找：Link集合名称可能带路径前缀，使用遍历+模糊匹配 |
| 2026-05-20 | 关联动画添加调试输出：打印集合名称、内容数量便于排查问题 |